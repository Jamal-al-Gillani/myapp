version: '3.8'

services:
  react-weather-app:
    build: .
    image: reactapp:01
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
